import { SlashCommandBuilder, PermissionFlagsBits, EmbedBuilder } from "discord.js";
export const data=new SlashCommandBuilder().setName("announce").setDescription("Publie une annonce structurée.").setDefaultMemberPermissions(PermissionFlagsBits.ManageMessages).addChannelOption(o=>o.setName("channel").setDescription("Salon cible").setRequired(true)).addStringOption(o=>o.setName("title").setDescription("Titre").setRequired(true)).addStringOption(o=>o.setName("message").setDescription("Message").setRequired(true));
export async function execute(i){const c=i.options.getChannel("channel",true);if(!c.isTextBased())return i.reply({content:"Salon incompatible.",ephemeral:true});await c.send({embeds:[new EmbedBuilder().setTitle(i.options.getString("title",true)).setDescription(i.options.getString("message",true)).setTimestamp().setFooter({text:`Publié par ${i.user.tag}`})]});await i.reply({content:"Annonce publiée.",ephemeral:true});}
